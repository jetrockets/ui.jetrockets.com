<%= container_tag do %>
  <div class="modal__content">
    <div class="modal__header">
      <div>
        <h3 class="modal__title">
          <%= @title %>
        </h3>
        <% if @subtitle %>
          <div class="modal__subtitle">
            <%= @subtitle %>
          </div>
        <% end %>
      </div>

      <% if helpers.respond_to?(:turbo_frame_request?) && helpers.turbo_frame_request? || !@async %>
        <button type="button" class="modal__close" data-action="click->modal-turbo#close click->modal#close" aria-label="Close" data-id="<%= @id if @id.present? %>">
          <%= helpers.vite_icon_tag "close.svg", class: "w-6 h-6" %>
        </button>
      <% end %>
    </div>

    <%= content %>
  </div>
<% end %>
