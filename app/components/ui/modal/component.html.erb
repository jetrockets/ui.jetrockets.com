<%= container_tag do %>
  <div class="modal__content">
    <div class="modal__header">
      <div>
        <h3 class="modal__title">
          <%= @title %>
        </h3>
        <% if @subtitle %>
          <div class="modal__subtitle">
            <%== @subtitle %>
          </div>
        <% end %>
      </div>

      <button type="button" class="modal__close" data-action="click->modal-async#close click->modal#close" aria-label="Close">
        <%= helpers.vite_svg_tag "images/icons/close.svg", class: "w-6 h-6" %>
      </button>
    </div>

    <%= content %>
  </div>
<% end %>
