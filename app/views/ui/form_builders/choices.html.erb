<%
  set_meta_tags(
    title: "Choices - Rails UI Components",
    description: "Enhanced select component with search functionality using Choices.js.",
    keywords: "Rails, UI Components, Choices, Select, Search, Form"
  )
%>
<% breadcrumb :ui_form_builders_choices %>

<article>
  <header class="header">
    <div class="header__heading">
      <h1 class="header__title">Choices</h1>
      <p class="header__subtitle">
        Enhanced select field with search functionality, multiple selection support, and custom options using Choices.js library.
      </p>
    </div>

    <div class="header__actions">
      <%= link_to "View on GitHub", "https://github.com/jetrockets/ui.jetrockets.com/tree/main/app/lib/form_builders/fields/choices_field.rb", class: "btn btn-secondary", target: "_blank", rel: "noopener noreferrer" %>
    </div>
  </header>

  <section>
    <h2 class="h3">Example</h2>
    <%= turbo_frame_tag :ui_form_builder, data: { turbo_action: :advance }, target: :_top do %>
      <%= render Ui::Tabs::Component.new class: "mb-6" do |tabs| %>
        <% tabs.with_item title: "Preview", href: ui_form_builders_choices_path, data: { turbo_frame: :ui_form_builder }, active: !params[:code] %>
        <% tabs.with_item title: "Code", href: ui_form_builders_choices_path(code: true), data: { turbo_frame: :ui_form_builder }, active: params[:code] %>
      <% end %>

      <div class="flex flex-col gap-2">
        <% unless params[:code] %>
          <%= render "ui/form_builders/examples/choices" %>
        <% else %>
          <%= render_code_example("ui/form_builders/examples/_choices") %>
        <% end %>
      </div>
    <% end %>
  </section>
</article>