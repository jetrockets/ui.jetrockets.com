<%
  set_meta_tags(
    title: "Typography - Rails UI Components",
    description: "Typography styles and text formatting utilities.",
    keywords: "Rails, UI Components, Typography"
  )
%>
<% breadcrumb :ui_typography %>

<article>
  <header class="header">
    <div class="header__heading">
      <h1 class="header__title">Typography</h1>
      <p class="header__subtitle">
        Typography styles and text formatting utilities.
      </p>
    </div>

    <div class="header__actions">
      <%= ui.btn "View on GitHub", url: "https://github.com/jetrockets/ui.jetrockets.com/tree/main/app/assets/stylesheets/components/typography.css", variant: :secondary, target: "_blank", rel: "noopener noreferrer" %>
    </div>
  </header>

  <section>
    <h2 class="h3">Example</h2>
    <%= turbo_frame_tag :ui_typography, data: { turbo_action: :advance }, target: :_top do %>
      <%= ui.tabs class: "mb-6" do %>
        <%= ui.tabs_item title: "Preview", href: ui_typography_path, data: { turbo_frame: :ui_typography }, active: !params[:code] %>
        <%= ui.tabs_item title: "Code", href: ui_typography_path(code: true), data: { turbo_frame: :ui_typography }, active: params[:code] %>
      <% end %>

      <div class="flex flex-col gap-2">
        <% unless params[:code] %>
          <%= render "ui/examples/typography" %>
        <% else %>
          <%= render_code_example("ui/examples/_typography") %>
        <% end %>
      </div>
    <% end %>
  </section>
</article>
