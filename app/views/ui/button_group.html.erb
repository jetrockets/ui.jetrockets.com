<%
  set_meta_tags(
    title: "Button Group - Rails UI Components",
    description: "Button Group component for grouping related buttons together.",
    keywords: "Rails, UI Components, Button Group"
  )
%>
<% breadcrumb :ui_button_group %>

<article>
  <header class="header">
    <div class="header__heading">
      <h1 class="header__title">Button Group</h1>
      <p class="header__subtitle">
        Button Group component for grouping related buttons together.
      </p>
    </div>

    <div class="header__actions">
      <%= ui.btn "View on GitHub", url: "https://github.com/jetrockets/ui.jetrockets.com/tree/main/app/components/ui/btn_group", variant: :secondary, target: "_blank", rel: "noopener noreferrer" %>
    </div>
  </header>

  <section>
    <h2 class="h3">Example</h2>
    <%= turbo_frame_tag :ui_button_group, data: { turbo_action: :advance }, target: :_top do %>
      <%= ui.tabs class: "mb-6" do %>
        <%= ui.tabs_item title: "Preview", href: ui_button_group_path, data: { turbo_frame: :ui_button_group }, active: !params[:code] %>
        <%= ui.tabs_item title: "Code", href: ui_button_group_path(code: true), data: { turbo_frame: :ui_button_group }, active: params[:code] %>
      <% end %>

      <div class="flex flex-col gap-2">
        <% unless params[:code] %>
          <%= render "ui/examples/button_group" %>
        <% else %>
          <%= render_code_example("ui/examples/_button_group") %>
        <% end %>
      </div>
    <% end %>
  </section>
</article>
