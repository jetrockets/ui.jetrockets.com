<%
  set_meta_tags(
    title: "Introduction - Rails UI Components",
    description: "A comprehensive Rails 7 component library built with ViewComponent, TailwindCSS 4.0, and Stimulus.",
    keywords: "Rails, UI, Components, ViewComponent, TailwindCSS, Stimulus"
  )
%>

<article class="wysiwyg">
  <h1 class="h1">JetRockets Kit for Ruby on Rails</h1>
  <p class="text-xl text-gray-600">
    A comprehensive Rails 7 component library built with ViewComponent, TailwindCSS 4.0, and Stimulus.
    This template provides a complete set of reusable UI components that developers can copy and customize for their projects.
  </p>

  <section class="space-y-4">
    <h2 class="h2">Key Technologies</h2>

    <div class="grid gap-4 md:grid-cols-2">
      <%= render Ui::Card::Component.new do |component| %>
        <% component.with_header align: :left, bordered: false, title: "Rails 7 or higher" %>
        <% component.with_body do %>
          <p>
            <%= link_to "Backend framework", "https://rubyonrails.org/", target: "_blank", rel: "noopener noreferrer" %>
            with modern features
          </p>
        <% end %>
      <% end %>

      <%= render Ui::Card::Component.new do |component| %>
        <% component.with_header align: :left, bordered: false, title: "ViewComponent" %>
        <% component.with_body do %>
          <p>
            <%= link_to "Component-based architecture", "https://viewcomponent.org/", target: "_blank", rel: "noopener noreferrer" %>
            for views
          </p>
        <% end %>
      <% end %>

      <%= render Ui::Card::Component.new do |component| %>
        <% component.with_header align: :left, bordered: false, title: "TailwindCSS 4.0" %>
        <% component.with_body do %>
          <p>
            <%= link_to "Utility-first CSS framework", "https://tailwindcss.com/", target: "_blank", rel: "noopener noreferrer" %>
            for rapid UI development
          </p>
        <% end %>
      <% end %>

      <%= render Ui::Card::Component.new do |component| %>
        <% component.with_header align: :left, bordered: false, title: "Stimulus" %>
        <% component.with_body do %>
          <p>
            <%= link_to "Modest JavaScript framework", "https://stimulus.hotwired.dev/", target: "_blank", rel: "noopener noreferrer" %>
            for enhancing HTML with minimal JavaScript
          </p>
        <% end %>
      <% end %>

      <%= render Ui::Card::Component.new do |component| %>
        <% component.with_header align: :left, bordered: false, title: "Flowbite" %>
        <% component.with_body do %>
          <p>
            <%= link_to "TailwindCSS components", "https://flowbite.com/", target: "_blank", rel: "noopener noreferrer" %>
            for ready-to-use UI elements and interactivity
          </p>
        <% end %>
      <% end %>

      <%= render Ui::Card::Component.new do |component| %>
        <% component.with_header align: :left, bordered: false, title: "Vite" %>
        <% component.with_body do %>
          <p>
            <%= link_to "Fast build tool", "https://vitejs.dev/", target: "_blank", rel: "noopener noreferrer" %>
            for frontend assets
          </p>
        <% end %>
      <% end %>
    </div>
  </section>

  <section class="space-y-4">
    <h2 class="text-2xl font-semibold text-gray-900">Next Steps</h2>

    <div class="space-y-3">
      <div class="flex items-center space-x-3">
        <div class="flex items-center justify-center w-8 h-8 text-sm font-semibold text-blue-600 bg-blue-100 rounded-full">1</div>
        <div>
          <%= link_to "Read Getting Started guide", ui_getting_started_path, class: "text-blue-600 hover:text-blue-800 font-medium" %> to learn how to use and customize components.
        </div>
      </div>

      <div class="flex items-center space-x-3">
        <div class="flex items-center justify-center w-8 h-8 text-sm font-semibold text-blue-600 bg-blue-100 rounded-full">2</div>
        <div>
          Explore the <strong>Components</strong> section in the sidebar to see detailed examples and usage patterns.
        </div>
      </div>

      <div class="flex items-center space-x-3">
        <div class="flex items-center justify-center w-8 h-8 text-sm font-semibold text-blue-600 bg-blue-100 rounded-full">3</div>
        <div>
          Check <%= link_to "Good to know", ui_good_to_know_path, class: "text-blue-600 hover:text-blue-800 font-medium" %> for best practices and architectural decisions.
        </div>
      </div>
    </div>
  </section>
</article>
